3)Eplain about shouldComponentUpdate in updating phase?


      => In the shouldComponentUpdate() method you can return a Boolean value that specifies whether React should continue 
with the rendering or not.

      => The default value is true.

      => The example below shows what happens when the shouldComponentUpdate() method returns false